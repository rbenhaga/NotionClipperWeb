# Supabase Configuration (PUBLIC - safe to expose)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# OAuth Client IDs (PUBLIC - safe to expose)
VITE_NOTION_CLIENT_ID=your-notion-client-id
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Stripe (PUBLIC - safe to expose)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_publishable_key

# App Configuration
VITE_APP_URL=http://localhost:5173
VITE_API_URL=http://localhost:5173

# IMPORTANT:
# All secrets (CLIENT_SECRET, STRIPE_SECRET_KEY, etc.) are stored in Supabase Vault
# and accessed only by Edge Functions server-side. Never expose secrets in the frontend.
