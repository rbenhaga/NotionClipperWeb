# Backend API URL (points to backend VPS)
VITE_API_URL=http://localhost:3001/api

# App Configuration
VITE_APP_URL=http://localhost:5173

# IMPORTANT:
# All OAuth secrets and Stripe keys are managed by the backend VPS.
# The backend fetches them from Supabase Vault via the get-oauth-secrets Edge Function.
# Never expose secrets in the frontend.
